export type movies = {
  id: string;
  title: string;
  poster_path: string;
  release_date: string;
  onClickLearn: any;
  onClickShare: any;
  learn: string;
  share: string;
};

export type DetailMovie = {
  // id: string;
  title: string;
  // poster_path: string;
  // release_date: string;
  // onClick: any;
};
