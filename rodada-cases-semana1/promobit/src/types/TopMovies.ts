export type moviesProps = {
  title: string;
  poster_path: string;
  release_date: string;
};

export type movies = {
  id: string;
  title: string;
  poster_path: string;
  release_date: string;
  onClick: any;
};

export type DetailMovie = {
  // id: string;
  title: string;
  // poster_path: string;
  // release_date: string;
  // onClick: any;
};
